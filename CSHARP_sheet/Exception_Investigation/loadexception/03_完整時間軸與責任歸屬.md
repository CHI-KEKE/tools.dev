# 完整時間軸與責任歸屬

## 本案（VSTS604561）的時間軸

```
2014-11-26  Eric Hsu
  VSTS：BTS5264 - 建立 NineYi.ERP.Backend.BLV2 相關 Services
  動作：建立 BLV2 專案，引入 MVC 4.0 + Razor 2.0
  影響：正常，版本配套正確

2020-10-20  Bill Chang
  VSTS：VSTS119687 - 新增 RazorEngine
  動作：安裝 RazorEngine 3.10.0
  影響：Razor 3.0 進入 bin，蓋掉 2.0 → 定時炸彈埋下
        但因為沒有任何呼叫觸發 ObjectContext.CreateQuery，所以沒有爆發

2025-02-17  peterjiyude
  VSTS：VSTS467969 - 調整一次取得一批自訂包裝資訊，避免影響效能
  動作：在 SalesOrderGroupRepository 加入 cfn_NvarcharSplitToVarcharTable（EF TVF）
  影響：第一個在 ERPDBEntitiesV2 上使用 ObjectContext.CreateQuery 的呼叫
        但被 IsEnableCustomPackaging feature flag 擋住（通常 false）→ 繼續沉睡

2026-04-29  Allen Lin
  VSTS：VSTS604561 - 加購欄位區分滿額/滿件
  動作：新增 CartExtraPurchase 查詢邏輯，呼叫 GetOrderCustomInfos（cfn 版）
  影響：第一個「沒被 feature flag 擋住」的 ObjectContext.CreateQuery 觸發
        → ReflectionTypeLoadException 爆發
```

## 責任釐清

| 人員 | 做了什麼 | 有沒有錯 |
|------|---------|---------|
| Eric Hsu | 建立專案，引入 MVC 4.0 | ❌ 沒有錯，當時配套正確 |
| Bill Chang | 引入 RazorEngine 3.10.0 | ⚠️ 沒有意識到 Razor 版本會衝突，但引入 Email 功能本身合理 |
| peterjiyude | 用 cfn EF TVF 查詢 | ⚠️ 沒有錯，但選用了會觸發 EF 掃描的 API |
| Allen Lin | 第一個觸發者 | ❌ 沒有錯，只是踩到了 5 年前的炸彈 |

**結論：我們不是引入問題的人，我們只是第一個觸發它的人。**

## Git 查詢指令

```powershell
# 查詢 RazorEngine 引入 commit
git log --all -S "RazorEngine" --format="%H %an %ad %s" --date=short -- "ERP/Backend/BLV2/packages.config"

# 查詢 cfn 引入 commit
git log --all -S "cfn_NvarcharSplitToVarcharTable" --format="%H %an %ad %s" --date=short -- "ERP/DA/ERPDBV2/Repositories/SalesOrderGroupRepository.cs"

# 查詢 BLV2 packages.config 最初建立 commit
git log --all --diff-filter=A --format="%H %an %ad %s" --date=short -- "ERP/Backend/BLV2/packages.config"
```

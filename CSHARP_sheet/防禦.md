


## collection 內可能有空的或 null 資料

```csharp
var couponShippingAreas = coupon.DeliveryList?
    .Where(x => string.IsNullOrWhiteSpace(x.ShippingArea) == false)
```

## 字串轉數字


```csharp
.Select(x =>
{
    var trimmed = x.Trim();
    return long.TryParse(trimmed, out var areaId)
        ? (long?)areaId
        : null;
})
.Where(x => x.HasValue)
.Select(x => x.Value)
```


## 去重?

```csharp
.Distinct()
```

## 不重複?

```csharp
.ToHashSet()
```

## 預設集合?

```csharp
?? new HashSet<long>()
```


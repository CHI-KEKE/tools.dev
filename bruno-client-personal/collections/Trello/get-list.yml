info:
  name: get-list
  type: http
  seq: 10

http:
  method: GET
  url: "{{base-url}}/1/boards/66953e430a49e05420243066/lists?key={{key}}&token={{token}}"
  params:
    - name: key
      value: "{{key}}"
      type: query
    - name: token
      value: "{{token}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

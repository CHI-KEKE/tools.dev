info:
  name: create-done-list
  type: http
  seq: 5

http:
  method: POST
  url: "{{base-url}}/1/lists?name=DONE&idBoard={{boardNumber}}&key={{key}}&token={{token}}"
  params:
    - name: name
      value: DONE
      type: query
    - name: idBoard
      value: "{{boardNumber}}"
      type: query
    - name: key
      value: "{{key}}"
      type: query
    - name: token
      value: "{{token}}"
      type: query
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        pm.test("Status code is 200", function () {
            pm.response.to.have.status(200);
        });

        let response = pm.response.json();
        pm.collectionVariables.set('DoneListId',response.id);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

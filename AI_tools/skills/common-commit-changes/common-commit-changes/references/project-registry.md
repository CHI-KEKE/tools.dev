# Commit Format Registry (Template)

> **Note:** This file is a reference template only. The actual registry is stored at
> `~/.copilot/commit-registry.md` and persists across plugin updates.

Formats:
- `vsts` → `VSTS{id} - {message}`
- `conventional` → `{type}: {message}`

## Registry

| Repo Name | Format | Notes |
|-----------|--------|-------|
| my-backend-repo | vsts | 後端專案，無 conventional type 前綴 |

---

<!-- When adding a new entry, append a row to the table above. -->
<!-- Repo name = the root folder name of the git repository. -->

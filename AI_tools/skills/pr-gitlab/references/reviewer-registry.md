# GitLab Reviewers Registry (Template)

> **Note:** This file is a reference template only. The actual registry is stored at
> `~/.copilot/gitlab-reviewers-registry.md` and persists across plugin updates.

## Registry

| Repo Name | Mode | Reviewers | Notes |
|-----------|------|-----------|-------|
| my-project | default | john.doe,jane.smith | 後端專案 |
| another-repo | ask | | 每次詢問 |
| solo-project | none | | 個人專案不需 reviewer |

---

<!-- Modes: default (use listed reviewers), ask (prompt every time), none (skip) -->
<!-- Reviewers: comma-separated GitLab usernames -->
<!-- When adding a new entry, append a row to the table above. -->
<!-- Repo name = the root folder name of the git repository. -->

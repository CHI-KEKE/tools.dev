# 🔍 {ClassName}.{MethodName}() 方法邏輯盤點

> **一句話摘要**：{這個方法的核心業務目的，20 字以內}

## 方法基本資訊
- **所在類別**：`{ClassName}`（`{相對檔案路徑}`）
- **方法簽名**：`{public async Task<ReturnType> MethodName(ParamType param1, ParamType param2)}`
- **行數範圍**：第 {N} — {N} 行（共 {N} 行）
- **是否為介面實作**：{是（實作 `{IInterfaceName}.{MethodName}`）| 否}

### 建構子依賴（此類別注入的服務）
| 欄位名稱 | 類型 | 在此方法中的用途 |
|---|---|---|
| `_{fieldName}` | `{IServiceName}` | {此方法呼叫它做什麼} |
| `_{fieldName}` | `{IRepositoryName}` | {此方法呼叫它做什麼} |

*只列出「此方法有實際使用到」的依賴*

---

## 邏輯流程拆解

### Phase 1：{階段名稱（業務語言）}
> 📍 第 {N}–{N} 行

{用 2-3 句業務語言描述這個階段做了什麼}

**外部呼叫**：
- 呼叫 `_{dep}.{Method}({params})` → {取得什麼 / 達到什麼效果}

---

### Phase 2：{階段名稱}
> 📍 第 {N}–{N} 行

{描述}

**決策點**：
- 若 `{條件}` → {走哪條路，結果是什麼}
- 若 `{條件}` → {走哪條路，結果是什麼}

---

### Phase 3：{階段名稱}
> 📍 第 {N}–{N} 行

{描述}

**外部呼叫**：
- 呼叫 `_{dep}.{Method}({params})` → {效果}

---

*(依實際邏輯增減 Phase，邏輯單純時可合併為一段)*

---

## 關鍵決策點彙整

| 決策點 | 條件 | 成立時的行為 | 不成立時的行為 |
|---|---|---|---|
| {決策描述} | `{條件表達式或說明}` | {行為} | {行為} |
| 早期返回 | `{條件}` | 拋出 `{ExceptionType}` / 回傳 `{value}` | 繼續執行 |

*若無複雜決策點，填：「此方法為線性流程，無複雜條件分支。」*

---

## 外部依賴呼叫清單

| 呼叫 | 傳入 | 取回 | 失敗時的影響 |
|---|---|---|---|
| `_{dep}.{Method}()` | `{傳入的變數/值}` | `{回傳值及如何使用}` | {方法中斷 / 繼續執行 / 降級到 {fallback}} |

---

## 風險點

### ⚠️ 警告（需要注意）
- **{風險類型}**（第 {N} 行）：{風險描述} — {建議處置}

### ℹ️ 資訊（低風險但值得知道）
- **{風險類型}**（第 {N} 行）：{描述}

*若無風險點，填：「✅ 此方法無明顯風險點。」*

---

## 修改建議（選填）

*若在分析過程中發現明顯可以改善的地方，在此列出。若無，刪除此段。*

- {建議 1}
- {建議 2}

---
*盤點時間：{YYYY-MM-DD HH:MM}*

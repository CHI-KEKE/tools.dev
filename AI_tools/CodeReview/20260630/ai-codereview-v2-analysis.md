# AI Code Review V2 — 執行流程分析與 Linter Gap 分析

> 分析日期：2026-06-30  
> 對比對象：Linter（CLI）vs AI Code Review V2（Webhook Server）  
> 目前專案路徑：`C:\91APP\AI_Devs\codereview\gitlab-codereview\nineyi.ai.code.review.v2`

---

## 一、AI Code Review V2 — Bitbucket Webhook 完整執行流程

```mermaid
flowchart TD
    WH["🔔 Bitbucket Webhook\npullrequest:created / updated"]
    CTRL["🌐 WebhookController\nPOST /api/webhook/bitbucket"]
    FILTER["🔍 事件過濾\nState == OPEN?"]
    SKIP1["⏭️ 跳過\n200 OK"]
    CMD["📋 StartCodeReviewCommand\nRepo / PR# / DiffUrl"]
    FIRE["⚡ Fire-and-Forget\nTask.Run → 立即回 200"]
    SVC["⚙️ CodeReviewService\nStartAsync"]

    subgraph PRE["前置過濾"]
        TITLE["🏷️ PR Title 檢查\n含 SKIP / WIP / SyncCode?"]
        SKIP2["⏭️ 跳過整個 PR"]
    end

    subgraph DIFF["Diff 取得"]
        FETCH["📡 BitbucketClient\nFetchRawDiffAsync\n（DiffUrl → 302 → CDN）"]
        PARSE["✂️ ParseRawDiff\n切分多檔案"]
    end

    subgraph FILEGUARD["DiffHelper 三層過濾（每個檔案）"]
        G1["❓ HasAddedLines?\n只有刪除 → skip"]
        G2["❓ IsAutoGeneratedContent?\nauto-generated → skip"]
        G3["✂️ RemoveDeletedLines\n移除 - 行後送 Dify"]
    end

    subgraph RULES["Rules 套用"]
        MATCH["🔎 匹配 ApplyToRepos\n+ FilePatterns"]
        PARA["⚡ Task.WhenAll\n同一檔案多 Rule 平行"]
        DIFY["🤖 Dify API\nchat-messages"]
    end

    subgraph POST["結果聚合發文"]
        DEDUP["🔁 GroupBy 去重\nComment.Trim OrdinalIgnoreCase"]
        AGG["📝 BuildAggregatedComment\n單一留言 file/rule <details>"]
        COMMENT["💬 PostPullRequestCommentAsync\nBasic Auth / Bearer"]
    end

    WH --> CTRL --> FILTER
    FILTER -->|"不符合"| SKIP1
    FILTER -->|"通過"| CMD --> FIRE --> SVC
    SVC --> TITLE
    TITLE -->|"命中關鍵字"| SKIP2
    TITLE -->|"通過"| FETCH --> PARSE
    PARSE --> G1
    G1 -->|"無新增行"| SKIP2
    G1 -->|"有新增行"| G2
    G2 -->|"AutoGen"| SKIP2
    G2 -->|"一般檔案"| G3 --> MATCH --> PARA --> DIFY
    DIFY --> DEDUP --> AGG --> COMMENT

    style WH fill:#F5A623,color:#fff
    style CTRL fill:#7B68EE,color:#fff
    style FIRE fill:#F5A623,color:#fff
    style SVC fill:#4A90D9,color:#fff
    style G1 fill:#D9534F,color:#fff
    style G2 fill:#D9534F,color:#fff
    style G3 fill:#4A90D9,color:#fff
    style DIFY fill:#5BA85B,color:#fff
    style COMMENT fill:#5BA85B,color:#fff
    style SKIP1 fill:#555,color:#fff
    style SKIP2 fill:#555,color:#fff
```

---

## 二、Linter vs V2 — Bitbucket 流程對比

```mermaid
flowchart LR
    subgraph LINTER["🔧 Linter（CLI）"]
        L1["CI Pipeline 觸發\n--platform --name --pr"]
        L2["Attribute Scan\n+ YAML Scan\nrepoMappingRuleSetting.yaml"]
        L3["GetDiffItems\n副檔名 / 路徑 / AutoGen\n/ ContainsAddedLines\n/ RemoveDeletedLines"]
        L4["Dify API\n逐 Rule 序列執行"]
        L5["GroupBy 去重\n（Rule 內）"]
        L6["Priority 分組發文\nHigh / Medium / Low / Unknown\n各一則留言（Bitbucket 倒序）"]
    end

    subgraph V2["🚀 V2（Webhook Server）"]
        V1["Bitbucket Webhook\nPOST /api/webhook/bitbucket"]
        V2A["CodeReviewConfig.Rules\nJSON + ApplyToRepos"]
        V3["DiffHelper\nHasAddedLines ✅\nIsAutoGeneratedContent ✅\nRemoveDeletedLines ✅"]
        V4["Dify API\n同檔案 Rules Task.WhenAll 平行"]
        V5["GroupBy 去重\n（跨所有 Rule）"]
        V6["單一聚合留言\n<details> 折疊 file/rule"]
    end

    L1 -.->|"觸發方式不同"| V1
    L2 -.->|"設定方式不同\n效果相同"| V2A
    L3 -.->|"V2 已補齊 ✅"| V3
    L4 -.->|"V2 平行更快 ✅"| V4
    L5 -.->|"V2 跨 Rule 更強 ✅"| V5
    L6 -.->|"❌ V2 尚缺"| V6

    style L1 fill:#F5A623,color:#fff
    style V1 fill:#F5A623,color:#fff
    style L3 fill:#4A90D9,color:#fff
    style V3 fill:#5BA85B,color:#fff
    style L4 fill:#7B68EE,color:#fff
    style V4 fill:#5BA85B,color:#fff
    style L6 fill:#D9534F,color:#fff
    style V6 fill:#555,color:#fff
```

---

## 三、Gap 分析 — V2 還缺什麼

```mermaid
flowchart TD
    subgraph DONE["✅ 今日已補齊（2026-06-30）"]
        D1["RemoveDeletedLines\n送 Dify 前移除 - 行"]
        D2["HasAddedLines guard\n只有刪除不呼叫 Dify"]
        D3["IsAutoGeneratedContent guard\nAutoGen 檔案跳過"]
    end

    subgraph GAP["❌ 仍缺少 / 待確認"]
        G1["🔴 Inline Comments\nPostReviewCommentAsync 已實作\n但 CodeReviewService 尚未接入\n→ 目前只有 PR 層級留言"]
        G2["🟡 Priority 分組留言\nLinter 依 High/Medium/Low 各發一則\nV2 全部塞進同一則\n→ 長 PR 難以閱讀"]
        G3["🟢 Bitbucket Signature 驗證\nBitbucket Cloud 無內建 Signature\n→ N/A，架構上已預留欄位"]
    end

    subgraph NA["⚪ 刻意設計差異（非缺陷）"]
        N1["CI vs Webhook 觸發模式\nV2 改為 event-driven，更合理"]
        N2["YAML vs JSON 規則設定\n效果相同，V2 支援動態 reload"]
        N3["序列 vs 平行執行\nV2 平行更快"]
    end

    style D1 fill:#5BA85B,color:#fff
    style D2 fill:#5BA85B,color:#fff
    style D3 fill:#5BA85B,color:#fff
    style G1 fill:#D9534F,color:#fff
    style G2 fill:#F5A623,color:#fff
    style G3 fill:#555,color:#fff
    style N1 fill:#4A90D9,color:#fff
    style N2 fill:#4A90D9,color:#fff
    style N3 fill:#4A90D9,color:#fff
```

---

## 四、今日變更摘要

### 新增 `DiffHelper.cs`

路徑：`src/BusinessLogic/NineYi.Ai.CodeReview.Application/Utilities/DiffHelper.cs`

| 方法 | 說明 |
|------|------|
| `RemoveDeletedLines(diff)` | 移除 `-` 開頭的刪除行，`---` header 保留 |
| `HasAddedLines(diff)` | 確認 diff 含新增行，只有刪除則略過 |
| `IsAutoGeneratedContent(diff)` | 偵測前 20 行是否含 `<auto-generated>` 標記 |

### 修改 `CodeReviewService.cs`

在 `foreach diffFile` 迴圈加入三層 guard，送 Dify 前先套用 `RemoveDeletedLines`：

```csharp
// ❶ 無新增行 → skip
if (!DiffHelper.HasAddedLines(diffFile.Diff)) continue;

// ❷ Auto-generated → skip
if (DiffHelper.IsAutoGeneratedContent(diffFile.Diff)) continue;

// ❸ 移除刪除行後送 Dify
FileDiff = DiffHelper.RemoveDeletedLines(diffFile.Diff)
```

> `DiffHelper` 集中一處，Bitbucket / GitLab / GitHub 三平台共用，解決 Linter 各自散落重複實作的問題。

---

## 五、剩餘待辦

| 優先 | 項目 | 說明 |
|------|------|------|
| 🔴 高 | **Inline Comments 接入** | `PostReviewCommentAsync` 已實作但未 wire 進 `CodeReviewService`，目前僅 PR 層級留言 |
| 🟡 中 | **Priority 分組留言** | 長 PR 所有意見擠在一則 `<details>`，可依 High/Medium/Low 分則發送 |
| ⚪ N/A | Bitbucket Signature 驗證 | Bitbucket Cloud 本身不支援，架構預留即可 |

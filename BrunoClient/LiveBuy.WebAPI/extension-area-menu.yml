info:
  name: extension-area-menu
  type: http
  seq: 9

http:
  method: GET
  url: "{{base-url}}/api/home/extension-area-menu?shopId=2&type=ExtensionArea"
  headers:
    - name: Cookie
      value: "{{Cookie}}"
  params:
    - name: shopId
      value: "2"
      type: query
    - name: type
      value: ExtensionArea
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

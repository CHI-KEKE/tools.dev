info:
  name: cloud-settings
  type: http
  seq: 8

http:
  method: GET
  url: "{{base-url}}/api/home/cloud-settings"
  headers:
    - name: Cookie
      value: "{{Cookie}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: livesession-active
  type: http
  seq: 5

http:
  method: GET
  url: "{{base-url}}/api/live/sessions/active"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: delete-connections
  type: http
  seq: 17

http:
  method: DELETE
  url: "{{base-url}}/api/websocket/connections"
  body:
    type: json
    data: |-
      {
        "connectionId": "string"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

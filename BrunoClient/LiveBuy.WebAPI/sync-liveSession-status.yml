info:
  name: sync-liveSession-status
  type: http
  seq: 18

http:
  method: POST
  url: "{{base-url}}/api/live/sessions/{{liveSessionId}}/sync-live-status?shopId={{shopId}}"
  params:
    - name: shopId
      value: "{{shopId}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: get-shopInfo-list
  type: http
  seq: 1

http:
  method: POST
  url: "{{base-url}}/scm/v2/Shop/GetShopInfoList?t={{token}}&ts={{timestamp}}&s={{signature}}"
  params:
    - name: t
      value: "{{token}}"
      type: query
    - name: ts
      value: "{{timestamp}}"
      type: query
    - name: s
      value: "{{signature}}"
      type: query
  body:
    type: json
    data: |-
      {
        "UserId": 91273,
        "UserName":123456782,
        "CloudType": "",
        "ShopId":2
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

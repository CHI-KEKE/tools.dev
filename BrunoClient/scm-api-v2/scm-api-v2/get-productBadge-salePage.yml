info:
  name: get-productBadge-salePage
  type: http
  seq: 4

http:
  method: POST
  url: "{{base-url}}/scm/v2/ProductBadge/GetProductBadgeSalePage?t={{token}}&ts={{timestamp}}&s={{signature}}"
  params:
    - name: t
      value: "{{token}}"
      type: query
    - name: ts
      value: "{{timestamp}}"
      type: query
    - name: s
      value: "{{signature}}"
      type: query
  body:
    type: json
    data: |-
      {
          "shopId" : 4,
          "productBadgeId" : 7,
          "page" : 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

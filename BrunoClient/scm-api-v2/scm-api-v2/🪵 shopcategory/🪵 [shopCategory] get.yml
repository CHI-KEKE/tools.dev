info:
  name: 🪵 [shopCategory] get
  type: http
  seq: 18

http:
  method: POST
  url: "{{base-url}}/scm/v2/ShopCategory/Get?t={{token}}&ts={{timestamp}}&s={{signature}}"
  params:
    - name: t
      value: "{{token}}"
      type: query
    - name: ts
      value: "{{timestamp}}"
      type: query
    - name: s
      value: "{{signature}}"
      type: query
  body:
    type: json
    data: |-
      {
          "ShopId":2,
          "ShopCategoryId":3135
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

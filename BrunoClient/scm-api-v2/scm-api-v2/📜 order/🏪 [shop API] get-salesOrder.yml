info:
  name: 🏪 [shop API] get-salesOrder
  type: http
  seq: 4

http:
  method: POST
  url: "{{base-url}}/SalesOrder/Get?t={{token}}&ts={{timestamp}}&s={{signature}}"
  params:
    - name: t
      value: "{{token}}"
      type: query
    - name: ts
      value: "{{timestamp}}"
      type: query
    - name: s
      value: "{{signature}}"
      type: query
  body:
    type: json
    data: |-
      {
          "ShopId": 2,
          "TGCode": "TG241028T00009",
          "TMCode": "TM241028T00009",
          "TSCode": "TS241028T000009"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  {
      "ShopId": 83,
      "TGCode": "MG260127M00002",
      "TMCode": "MM260127M00002",
      "TSCode": "MS260127M000003"
  }

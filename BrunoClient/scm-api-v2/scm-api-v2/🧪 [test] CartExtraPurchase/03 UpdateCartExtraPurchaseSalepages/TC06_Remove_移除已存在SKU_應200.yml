info:
  name: TC06_Remove_移除已存在SKU_應200
  type: http
  seq: 6

meta:
  purpose: |-
    【正向】對 TC01 已加入活動的 SKU 發 ActionType=Remove。
    預期：200，SKU 從活動中移除。

http:
  method: POST
  url: "{{base-url}}/Promotion/UpdateCartExtraPurchaseSalepages?t={{token}}&ts={{timestamp}}&s={{signature}}"
  params:
    - name: t
      value: "{{token}}"
      type: query
    - name: ts
      value: "{{timestamp}}"
      type: query
    - name: s
      value: "{{signature}}"
      type: query
  body:
    type: json
    data: |-
      {
        "ShopId": 10230,
        "PromotionId": {{promotionId}},
        "IsAddHistory": false,
        "CartExtraPurchaseConditionList": [
          {
            "ActionType": "Remove",
            "ReachPiece": 2,
            "ReachPrice": 0,
            "SalepageId": 1557573,
            "SaleProductSKUId": 2169001,
            "CartExtraPurchasePrice": 0,
            "Sort": 1
          }
        ]
      }
  auth: inherit

tests: |-
  test("TC-SP06 狀態碼應為 200", function () {
    expect(res.getStatus()).to.equal(200);
  });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
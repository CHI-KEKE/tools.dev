info:
  name: translation-fetch
  type: http
  seq: 13

http:
  method: GET
  url: "{{base-url}}/translations/fetch"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

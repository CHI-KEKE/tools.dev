info:
  name: locationPickup-arrivedConfirm
  type: http
  seq: 3

http:
  method: GET
  url: "{{base-url}}/SCM/V2/LocationPickup/ArrivedConfirm?t={{token}}&ts={{timestamp}}&s={{signature}}"
  params:
    - name: t
      value: "{{token}}"
      type: query
    - name: ts
      value: "{{timestamp}}"
      type: query
    - name: s
      value: "{{signature}}"
      type: query
  body:
    type: json
    data: |-
      {
        "data": "{\"ShopId\":2,\"ShippingOrderCode\":\"L000000923\"}"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: notify
  type: http
  seq: 1

http:
  method: POST
  url: https://notify-api.line.me/api/notify?message=我是可愛的提醒小豬🐗
  params:
    - name: message
      value: 我是可愛的提醒小豬🐗
      type: query
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

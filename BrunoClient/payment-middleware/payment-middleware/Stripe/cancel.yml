info:
  name: cancel
  type: http
  seq: 6

http:
  method: POST
  url: "{{base-url}}/api/v1.0/Cancel/CreditCardOnce_Stripe/{{TG}}"
  body:
    type: json
    data: |-
      { 
         "request_id" :"{{$guid}}",
         "transaction_id":"{{transactionId}}",
         "extend_info": {
            "payment_flow": "{{flow}}",
            "stripe_account":"{{acct}}"
         }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

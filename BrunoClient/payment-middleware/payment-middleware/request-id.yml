info:
  name: request-id
  type: http
  seq: 2

http:
  method: GET
  url: "{{base-url}}/api/v1.0/RequestId"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

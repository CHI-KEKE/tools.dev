info:
  name: test
  type: http
  seq: 5

http:
  method: GET
  url: "{{base-url}}/{{vedio-id}}?metadata=1&access_token={{page-access-token}}"
  params:
    - name: metadata
      value: "1"
      type: query
    - name: access_token
      value: "{{page-access-token}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

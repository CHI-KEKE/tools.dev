info:
  name: subscribed_apps
  type: http
  seq: 3

http:
  method: GET
  url: "{{base-url}}/{{pageId}}/subscribed_apps?subscribed_fields=feed,live_videos,message_reactions,messages,messaging_postbacks&access_token={{page-access-token}}"
  params:
    - name: subscribed_fields
      value: feed,live_videos,message_reactions,messages,messaging_postbacks
      type: query
    - name: access_token
      value: "{{page-access-token}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: fixed-get
  type: http
  seq: 1

http:
  method: GET
  url: "{{base-url}}/api/fixed-salepage-collections/{{collectionId}}"
  headers:
    - name: N1-TRACE-ID
      value: "{{traceId}}"
    - name: N1-SHOP-ID
      value: "{{shopId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: histories-fromUpdateIndex-toUpdateIdx
  type: http
  seq: 7

http:
  method: GET
  url: "{{base-url}}/api/salepage-collections/{{collectionId}}/histories?fromUpdateIndex=2&toUpdateIdx=3"
  headers:
    - name: N1-TRACE-ID
      value: "{{traceId}}"
    - name: N1-SHOP-ID
      value: "{{shopId}}"
  params:
    - name: fromUpdateIndex
      value: "2"
      type: query
    - name: toUpdateIdx
      value: "3"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: promotion
  type: http
  seq: 4

http:
  method: GET
  url: "{{base-url}}/api/promotion-salepage-collections/{{promotionEngineId}}"
  headers:
    - name: N1-TRACE-ID
      value: "{{traceId}}"
    - name: N1-SHOP-ID
      value: "{{shopId}}"
  params:
    - name: ""
      value: ""
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: emoji.list
  type: http
  seq: 3

http:
  method: GET
  url: https://slack.com/api/emoji.list
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

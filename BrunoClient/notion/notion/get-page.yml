info:
  name: get-page
  type: http
  seq: 4

http:
  method: GET
  url: "{{base-url}}/pages/{{page-id}}"
  headers:
    - name: Notion-Version
      value: "{{version}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

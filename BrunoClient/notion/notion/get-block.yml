info:
  name: get-block
  type: http
  seq: 9

http:
  method: GET
  url: "{{base-url}}/blocks/{{block-id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

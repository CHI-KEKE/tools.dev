info:
  name: update-page
  type: http
  seq: 8

http:
  method: PATCH
  url: "{{base-url}}/pages/{{page-id}}"
  body:
    type: json
    data: |-
      {
        "properties": {
          "Species": {
            "select": {
              "name": "GoGoRo"
            }
          }
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: get-commits
  type: http
  seq: 2

http:
  method: GET
  url: "{{base-url}}/2.0/repositories/nineyi/nineyi.configuration/pullrequests/6/commits"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

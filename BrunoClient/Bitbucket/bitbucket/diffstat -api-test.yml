info:
  name: diffstat -api-test
  type: http
  seq: 11

http:
  method: GET
  url: "{{base-url}}/2.0/repositories/{{workspace}}/{{repo_slug}}/pullrequests/8167/diffstat?pagelen=100"
  params:
    - name: pagelen
      value: "100"
      type: query
  auth:
    type: basic
    username: "{{Username}}"
    password: "{{AccessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

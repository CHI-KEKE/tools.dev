info:
  name: sku-update-qty
  type: http
  seq: 3

http:
  method: POST
  url: "{{base-url}}/api/carts/sku-update-qty"
  headers:
    - name: N1-SHOP-ID
      value: "{{shopId}}"
    - name: Cookie
      value: "{{Cookie}}"
    - name: N1-HOST
      value: "{{host}}"
  params:
    - name: ""
      value: ""
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

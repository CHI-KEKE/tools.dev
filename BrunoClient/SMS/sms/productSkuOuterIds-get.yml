info:
  name: productSkuOuterIds-get
  type: http
  seq: 7

http:
  method: POST
  url: "{{base-url}}/Api/PromotionEngine/GetProductSkuOuterIds"
  headers:
    - name: Cookie
      value: "{{Cookie}}"
  body:
    type: json
    data: |-
      {
          "Skip":0,
          "Take":500,
          "SearchItem":{
             "ShopId":2,
             "PromotionEngineId":6552
          }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: get-salgpageInfo
  type: http
  seq: 1

http:
  method: GET
  url: "{{base-url}}/Api/SalePage/GetSalePageInfo/{{shopId}}/{{salepageId}}"
  headers:
    - name: Cookie
      value: "{{Cookie}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: get-by-etag
  type: http
  seq: 3

http:
  method: GET
  url: "{{template-base-url}}/api/Templates/get-by-etag?Name={{Name}}&ETag={{ETag}}&Lang={{Lang}}&ShopId={{ShopId}}"
  params:
    - name: Name
      value: "{{Name}}"
      type: query
    - name: ETag
      value: "{{ETag}}"
      type: query
    - name: Lang
      value: "{{Lang}}"
      type: query
    - name: ShopId
      value: "{{ShopId}}"
      type: query
  auth: inherit

runtime:
  variables:
    - name: Name
      value: TestSCMEmail_TradesOrderV2_RazorTemplate
    - name: ETag
      value: 79238dbf089998697e7ed6600c0366ff
    - name: Lang
      value: zh-CN
    - name: ShopId
      value: "5"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

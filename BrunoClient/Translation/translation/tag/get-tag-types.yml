info:
  name: get-tag-types
  type: http
  seq: 2

http:
  method: GET
  url: "{{base-url}}/api/v3/projects/tagtypes"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

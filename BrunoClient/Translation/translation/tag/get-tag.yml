info:
  name: get-tag
  type: http
  seq: 1

http:
  method: GET
  url: "{{base-url}}/api/v3/projects/{{project-name}}/tags/Page"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

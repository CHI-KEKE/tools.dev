info:
  name: get-project
  type: http
  seq: 5

http:
  method: GET
  url: "{{base-url}}/api/v3/projects/{{project-name}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: delete-project
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{base-url}}/api/v3/projects/{{project-id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

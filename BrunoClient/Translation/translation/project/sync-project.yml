info:
  name: sync-project
  type: http
  seq: 2

http:
  method: POST
  url: "{{base-url}}/api/v3/projects/{{project-name}}/sync"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

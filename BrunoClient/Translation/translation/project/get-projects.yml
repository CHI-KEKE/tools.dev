info:
  name: get-projects
  type: http
  seq: 4

http:
  method: GET
  url: "{{base-url}}/api/v3/projects"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

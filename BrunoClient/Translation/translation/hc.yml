info:
  name: hc
  type: http
  seq: 1

http:
  method: GET
  url: "{{base-url}}/api/translations/health"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

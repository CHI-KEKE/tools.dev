info:
  name: get-module-id
  type: http
  seq: 2

http:
  method: GET
  url: "{{base-url}}/api/v3/projects/{{project-name}}/modules/{{module-name}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

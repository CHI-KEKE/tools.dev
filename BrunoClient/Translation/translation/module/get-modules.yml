info:
  name: get-modules
  type: http
  seq: 1

http:
  method: GET
  url: "{{base-url}}/api/v3/projects/{{project-name}}/modules"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

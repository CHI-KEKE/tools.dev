info:
  name: version
  type: http
  seq: 4

http:
  method: GET
  url: "{{base-url}}/api/translations/version"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

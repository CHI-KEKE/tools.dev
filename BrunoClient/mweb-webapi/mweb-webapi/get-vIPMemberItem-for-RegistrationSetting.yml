info:
  name: get-vIPMemberItem-for-RegistrationSetting
  type: http
  seq: 9

http:
  method: GET
  url: "{{base-url}}/webapi/VIPMember/GetVIPMemberItemForRegistrationSetting?lang=zh-TW&shopId={{shopId}}"
  params:
    - name: lang
      value: zh-TW
      type: query
    - name: shopId
      value: "{{shopId}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

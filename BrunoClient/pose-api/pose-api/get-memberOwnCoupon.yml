info:
  name: get-memberOwnCoupon
  type: http
  seq: 3

http:
  method: POST
  url: "{{base-url}}/pos/v1/GetMemberOwnCoupon?t={{token}}&ts={{timestamp}}&s={{signature}}"
  params:
    - name: t
      value: "{{token}}"
      type: query
    - name: ts
      value: "{{timestamp}}"
      type: query
    - name: s
      value: "{{signature}}"
      type: query
  body:
    type: json
    data: "{{request_body}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
